<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Zoo Arcadia{% endblock %}</title>
    <link rel="icon" type="image/jpeg" href="{{ asset('favicon.ico') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        header .nav-link {
            color: white;
            background-color: rgba(159, 107, 147, 0.77);
            margin: 0 5px;
            padding: 6px 14px;
            border-radius: 5px;
        }
        header .nav-link:hover {
            background-color: rgba(225, 106, 199, 0.58);
        }
        body {
            background-color: rgba(116, 23, 85, 0.9), rgba(234, 136, 201, 0.99);
        }
        footer {
            background-color: rgba(235, 191, 232, 0.78);
            padding: 2rem;
            border-top: 1px solid #ddd;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
<header class="p-3 border-bottom">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <img src="{{ asset('uploads/logo.jpg') }}" style="height: 60px;">
        <button class="navbar-toggler d-md-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarBurger">
            <span class="navbar-toggler-icon"></span>
        </button>
        <nav class="collapse navbar-collapse d-md-flex justify-content-end" id="navbarBurger">
            <ul class="navbar-nav d-flex flex-md-row">
                <li class="nav-item"><a href="{{ path('home') }}" class="nav-link">Accueil</a></li>
                <li class="nav-item"><a href="{{ path('habitats') }}" class="nav-link">Habitats</a></li>
                <li class="nav-item"><a href="{{ path('services') }}" class="nav-link">Services</a></li>
                <li class="nav-item"><a href="{{ path('contact') }}" class="nav-link">Contact</a></li>
                <li class="nav-item"><a href="{{ path('login') }}" class="nav-link">Connexion</a></li>
            </ul>
        </nav>
    </div>
</header>

<main class="container mt-4">
    {% block body %}{% endblock %}
</main>

<footer class="text-center">
    <div class="row">
        <div class="col-md-4">
            <h6>ADRESSE</h6>
            <p>25 rue du zoo<br>Forêt d'Arcadia<br>01 30 39 37 45</p>
        </div>
        <div class="col-md-4">
            <h6>NOS HORAIRES</h6>
            <p>Lun-Dim : 9h - 15h</p>
        </div>
        <div class="col-md-4">
            <h6>MENU</h6>
            <p>Accueil · Habitats · Services · Contact · Connexion</p>
        </div>
    </div>
    <p class="mt-3">
        <a href="#">Politique de confidentialité</a> ·
        <a href="#">Mentions légales</a>
    </p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

