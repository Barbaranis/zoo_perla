<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Zoo Arcadia{% endblock %}</title>
    <meta name="description" content="Découvrez Zoo Arcadia : un univers naturel, magique et engagé pour la biodiversité.">
    <link rel="icon" type="image/jpeg" href="{{ asset('favicon.ico') }}">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">


    <style>
    :root {
        --vert-nature: #3b5d3b;
        --brun-nature: #8f735b;
        --orange-doux: #e6ae78;
        --beige-nature: #fdfaf4;
        --blanc-creme: #f5f5f5;
        --marron-fonce: #4a342e;
    }


    body {
        background: linear-gradient(to bottom right, var(--vert-nature), var(--brun-nature), var(--orange-doux));
        color: var(--blanc-creme);
        font-family: 'Segoe UI', sans-serif;
    }


    header {
        background: linear-gradient(to right, rgba(59, 93, 59, 0.95), rgba(85, 63, 50, 0.95));
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        border-bottom: 2px solid var(--orange-doux);
        padding: 1.5rem 0;
        backdrop-filter: blur(4px);
    }


    .navbar {
        background: linear-gradient(to right, var(--vert-nature), var(--brun-nature));
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        padding: 1.2rem 0;
        border-bottom: 2px solid var(--orange-doux);
    }


    .navbar-brand img {
        height: 60px;
        border-radius: 10px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
    }


    .nav-link {
        color: var(--blanc-creme);
        margin: 0 10px;
        padding: 0.6rem 1.2rem;
        font-weight: 600;
        border-radius: 8px;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }


    .nav-link::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: var(--orange-doux);
        border-radius: 8px;
        z-index: -1;
        opacity: 0;
        transform: scale(0.95);
        transition: all 0.3s ease-in-out;
    }


    .nav-link:hover::before {
        opacity: 1;
        transform: scale(1);
    }


    .nav-link:hover {
        color: var(--marron-fonce);
        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.3);
    }


    footer {
        background: linear-gradient(to right, rgba(85, 63, 50, 0.9), rgba(93, 81, 59, 0.9));
        color: var(--blanc-creme);
        padding: 2rem;
        border-top: 2px solid var(--orange-doux);
        margin-top: 5%;
    }


    footer h6 {
        font-weight: bold;
    }


    footer a {
        color: var(--orange-doux);
        text-decoration: none;
    }


    footer a:hover {
        text-decoration: underline;
    }


    @media (max-width: 768px) {
        .navbar-brand img {
            height: 60px;
        }
    }
    </style>


    {% block stylesheets %}{% endblock %}
</head>


<body>
<header class="p-3 border-bottom">
    <div class="container-fluid">
        <nav class="navbar navbar-expand-md navbar-dark">
            <a class="navbar-brand" href="{{ path('home') }}">
                <img src="{{ asset('uploads/logo.jpg') }}" alt="Logo Zoo Arcadia">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a href="{{ path('home') }}" class="nav-link">Accueil</a></li>
                    <li class="nav-item"><a href="{{ path('habitats') }}" class="nav-link">Habitats</a></li>
                    <li class="nav-item"><a href="{{ path('services') }}" class="nav-link">Services</a></li>
                    <li class="nav-item"><a href="{{ path('contact') }}" class="nav-link">Contact</a></li>
                    <li class="nav-item"><a href="{{ path('login') }}" class="nav-link">Connexion</a></li>
                </ul>
            </div>
        </nav>
    </div>
</header>


<main>
    {% block body %}{% endblock %}
</main>


<footer class="text-center">
    <div class="container">
        <div class="row">
            <div class="col-md-4 mb-4">
                <h6>Adresse</h6>
                <p>12 rue de l'invention<br>Forêt de Brocéliande<br>01 30 39 37 45</p>
            </div>
            <div class="col-md-4 mb-4">
                <h6>Horaires</h6>
                <p>Lundi au Dimanche<br>de 9h à 15h</p>
            </div>
            <div class="col-md-4 mb-4">
                <h6>Navigation</h6>
                <p>Accueil · Habitats · Services · Contact · Connexion</p>
            </div>
        </div>
        <p class="mt-3 mb-0">
            <a href="{{ path('privacy') }}">Politique de confidentialité</a> ·
            <a href="#">Mentions légales</a>
        </p>
    </div>
</footer>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>


<!-- COOKIES -->
<div id="cookie-banner" style="display:none; position: fixed; bottom: 0; width: 100%; background-color: #3b5d3b; color: white; padding: 1rem; text-align: center; z-index: 9999;">
    Ce site utilise des cookies pour vous garantir la meilleure expérience.
    <button onclick="acceptCookies()" style="margin-left: 1rem; background-color: #fff; color: #3b5d3b; border: none; padding: 0.5rem 1rem; border-radius: 5px;">
        Accepter
    </button>
</div>


<script>
    function acceptCookies() {
        localStorage.setItem("cookieAccepted", "true");
        document.getElementById("cookie-banner").style.display = "none";
    }
    window.onload = function () {
        if (!localStorage.getItem("cookieAccepted")) {
            document.getElementById("cookie-banner").style.display = "block";
        }
    }
</script>


</body>
</html>

