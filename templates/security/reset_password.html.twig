{% extends 'base.html.twig' %}

{% block title %}Réinitialisation - Zoo Arcadia{% endblock %}

{% block stylesheets %}
<style>
    .reset-container {
        min-height: 85vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(to left, #3b5d3b, #8f735b);
        color: white;
        padding: 2rem;
    }
    .reset-card {
        background-color: #fdfaf4;
        color: #333;
        padding: 2rem;
        border-radius: 1rem;
        max-width: 500px;
        width: 100%;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }
    .reset-card h2 {
        color: #3b5d3b;
    }
    .btn-reset {
        background-color: #3b5d3b;
        color: white;
        width: 100%;
        border-radius: 8px;
        padding: 0.6rem;
        transition: all 0.3s ease;
    }
    .btn-reset:hover {
        background-color: #2d4a2d;
        transform: scale(1.03);
    }
</style>
{% endblock %}

{% block body %}
<div class="reset-container">
    <div class="reset-card">
        <h2 class="text-center mb-4">Réinitialisez votre mot de passe</h2>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}
        {% for message in app.flashes('danger') %}
            <div class="alert alert-danger">{{ message }}</div>
        {% endfor %}

        {{ form_start(resetForm) }}
            <div class="mb-3">
                {{ form_label(resetForm.plainPassword) }}
                {{ form_widget(resetForm.plainPassword) }}
                {{ form_errors(resetForm.plainPassword) }}
            </div>
            <button type="submit" class="btn btn-reset">Changer le mot de passe</button>
        {{ form_end(resetForm) }}
    </div>
</div>
{% endblock %}

